{
  "command": "repeat",
  "description": "Repeats the OAuth flow at specified intervals",
  "options": [
    {
      "name": "interval",
      "description": "Interval in minutes",
      "required": false
    }
  ]
}
